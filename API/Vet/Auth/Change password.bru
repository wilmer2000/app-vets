meta {
  name: Change password
  type: http
  seq: 2
}

post {
  url: {{apiUrl}}/auth/change-password
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@email.com",
    "password": "passwordTest"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
